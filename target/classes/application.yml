spring:
  datasource:
    url: jdbc:mysql://db4free.net:3306/team202505?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: user202505
    password: 123456789
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        globally_quoted_identifiers: true
    database-platform: org.hibernate.dialect.MariaDBDialect

# 添加日志配置
logging:
  level:
    root: INFO
    uk.ac.ncl.team5project: DEBUG
  file:
    name: logs/application.log
    max-size: 10MB
    max-history: 7

server:
  port: 8080 

mybatis-plus:
  mapper-locations: classpath*:mapper/*.xml
  global-config:
    db-config:
      id-type: auto
      logic-delete-value: 1
      logic-not-delete-value: 0
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: MySecretKeyKK # 盐值 salt。炒菜 不同的菜加的盐不一样 就能保证安全了。因为你无法反推
  expiration: 86400000 # 1天 (毫秒)  